// define total and limit
var total = 0;
var limit = 10;

// loop to add 1 to total for each pass through up to limit
for ( var i = 0; i < limit; i ++) {
  total += i;
}
// print new total to console
console.log(total);
